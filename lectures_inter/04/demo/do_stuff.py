#!/usr/bin/env python3
# from fruit import write_fruits

if __name__ == "__main__":
    # import argparse
    # parser = argparse.ArgumentParser()
    # parser.add_argument("--filename", "-f", help="The file to write the fruits to", default="fruits.txt")
    # file = parser.parse_args().filename
    # write_fruits(parser.parse_args().filename)
    print("I'm doing stuff!")