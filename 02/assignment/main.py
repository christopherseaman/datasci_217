from analysis_utils import format_summary


def main():
    """TODO: describe the report workflow."""
    # SUPPLIED RECORDS: do not edit this block.
    records = [
        {"label": "Morning", "values": [18, 21, 24]},
        {"label": "Evening", "values": [20, 22, 26]},
        {"label": "Overnight", "values": []},
    ]
    # END SUPPLIED RECORDS

    # TODO: Build report_text by calling format_summary() once per record.
    # TODO: In one with block, open mode "w" and call report_file.write(report_text).
    # TODO: In a second with block, reopen mode "r" and assign report_file.read().
    # TODO: Print the saved report and an inline or named equality result.
    pass


if __name__ == "__main__":
    main()
