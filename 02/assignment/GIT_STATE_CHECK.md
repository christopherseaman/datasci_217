# Assignment 02 Git state snapshots

Use only the Lecture 02 terms named in each prompt. Replace the `TODO` entries in the answer block; keep the numbering and semicolon separators.

1. You save an edit to `README.md` but have not staged it. Which Git state contains the edit, and what line-by-line view lets you inspect it? Use `working tree` and `diff`.
2. You select that change for the next recorded snapshot. Which state now proposes its content, and what snapshot is created after you select Commit? Use `staging area` and `commit`.
3. A new commit exists on your current branch but is not yet on GitHub. Which two copies differ, and which GUI action coordinates the needed transfer? Use `local branch`, `remote`, and `synchronize`.
4. You combine a feature branch into `main`, but Git cannot safely combine one overlapping edit. Name the combine operation and the state that must be resolved. Use `merge` and `conflict`.

<!-- ANSWERS START -->
1. TODO; TODO
2. TODO; TODO
3. TODO; TODO; TODO
4. TODO; TODO
<!-- ANSWERS END -->
