# Assignment 02 grading development self-test

This development QA smoke test copies the current assignment into ignored `scratch/`, accepts its saved report, then confirms a changed report is rejected. It is not a separate scoring mode; graders use the public checker from a trusted copy.

```bash
python 02/assignment/_grader_selftest/run.py
```
