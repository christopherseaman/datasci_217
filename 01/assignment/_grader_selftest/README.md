# Assignment 01 grader self-test

This instructor-only smoke test copies the current assignment into ignored `scratch/`, accepts its saved readiness artifact, then confirms a changed artifact is rejected.

```bash
python 01/assignment/_grader_selftest/run.py
```
