# Assignment 03 grader self-test

This instructor-only smoke test copies the current assignment into ignored `scratch/`, accepts its saved pipeline results and analysis transcript, then confirms a changed artifact is rejected.

```bash
python 03/assignment/_grader_selftest/run.py
```
