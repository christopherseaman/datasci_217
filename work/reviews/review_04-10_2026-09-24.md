# Lectures 04-10 review, 2026-09-24

Review only; nothing was changed. [L] lecture, [D] demos, [A] assignment.

## Lecture 04

The lecture's order and demo breaks are sound, and every snippet reproduces. It has one coverage gap (summary statistics) and a missing kernel-setup step. Demos 2 and 3 cannot run in Colab as linked and do not use health data. The assignment handout ships its answer key (see patterns).

- [L] 04/README.md:610-618: add `df['col'].mean()/.min()/.max()`, `df.mean(numeric_only=True)` and `idxmax()` to the First-look card, plus a snippet on `visits['temp_c']` (37.400000000000006, 38.4, 3) that says pandas skips NaN where NumPy returns nan. 05/README.md:47 already relies on this.
- [L] 04/README.md:22-40: add one sentence and one card row saying VS Code needs the Jupyter extension and `ipykernel` in the .venv (`uv pip install ipykernel`).
- [L] 04/README.md:190-192 and :516-518: move the italic comic captions into the image link text (`![xkcd 2180: Spreadsheets. ...]`) and delete :192 and :518.
- [L] 04/README.md:18 and :155-159: open the topic with the :20 definition, not a recap. State plainly that saved outputs go into the .ipynb and Git history. Move the :644 Git quote to after :184.
- [L] 04/README.md:198: drop the inline McKinney citation. At :201 and :596, change `*...*` to `_..._`. Delete :344, which repeats :339-342 and :380.
- [D] demo2_pandas_basics.md:54-86 and demo3_data_io.md:61-115: recast outdoor temps, unitless obs and Anscombe as patient temps and SBP in mmHg, using a committed 04/demo/data/clinic_visits.csv modeled on README:532-539.
- [D] demo3_data_io.md:56-109: replace the hashlib/urllib/importlib bootstrap with `pd.read_csv(DATA_URL)` (taught at README:522). In demo1:101-106, replace `tempfile` with `Path('output')` (HANDOFF 04-15).
- [D] demo1:32-36 and :121, demo2:29, demo3:30: replace the 'Compatibility candidate'/`PANDAS_CANDIDATE` authoring text with one student line. Swap `np.array_equal`/`assert_frame_equal`/`np.allclose` for `.equals()`. Add 'Expect:' lines to Demo 2 and a Clear All Outputs step to Demo 1.
- [A] check_assignment.py:96-152: split the 60-point selected_purchases check into independent 15-point checks that all run. Recast purchases as a clinic supply order and site temps as vaccine-fridge readings (2-8 C). Setup: add fork/clone/git config and ipykernel, and use plain wording (README:14-29).

## Lecture 05

The lecture is in good shape: 18 snippets verified, 831 lines, even blocks. One weak topic and some misplaced jokes remain. Its demos are the weakest in 04-10: Demo 3 does not practice block 3 at all. Assignment changes are listed under midterm_05.

- [L] 05/README.md:320-367: rewrite Applying Custom Functions around a clinical problem: `sbp.apply(bp_category)`, `smoking.map({...})`, and `axis=1` only for a two-column condition (sbp>=140 or dbp>=90). Add one sentence that plain column arithmetic needs no apply.
- [L] 05/README.md:20 and :577: delete the Lecture 04 recaps. Start :577 at 'Inspection describes a table; **validation**...'.
- [L] 05/README.md:76, :224, :276, :408, :450, :503: cut the in-topic jokes that restate content. Keep :18, :68 and :184 at topic boundaries, and change :224 and :450 to `_..._`.
- [L] 05/README.md:70: the caption should name MCAR/MAR/MNAR and the point of the figure. Delete :373 (undefined 'Copy-on-Write'). At :525, the memory figure depends on pyarrow (59,132 with, 264,132 without).
- [L] Repo root clean_patients.csv (tracked) is snippet output from README:790-803; `git rm` it.
- [D] demo3_workflow.md: rebuild on clinic visits to practice block 3: raw read with `keep_default_na=False` plus a snapshot, `between`/`isin`/`fullmatch` failures, an IQR flag, `sample(random_state=42)`, a decision table, and an invariants assert above `to_csv` (one labelled failure) followed by a round-trip `.equals()` (HANDOFF 05-A, 05-04).
- [D] demo1_missing_data.md:62-91: delete the concat/rename/drop/heatmap section (untaught, 05-B). Add a duplicate row, a -999 sentinel, 'forty'/'unknown' ages, '2024-02-30', and a row missing both BP and cholesterol, so that `parse_failed` and the drop step visibly change output (05-16).
- [D] demo2_transformations.md:34-67: recast the survey as a clinic intake export with space-laden headers. Leave coerced ages missing instead of imputing and then binning them; at the moment 'thirty-two' and 'invalid' both become 33.8.

## Lecture 06

The structure is sound and all snippets verified. The worked examples drift away from the health problems each intro promises, and the pivot-duplicate failure is prose only. The demos are retail/HR data, contain factual errors, and skip the safeguards the assignment grades.

- [L] 06/README.md:24, :35-99: draw the join diagram and the first two snippets from one patients/labs dataset with identical rows. The diagram (customers 1-3) does not currently match the snippet (C001-C005).
- [L] 06/README.md:478-541: the intro and :541 promise the baseline_sbp/followup_sbp table, but the snippets use Alice/Bob subject scores (confirmed). Recast wide_data/long_data to patient_id, visit, sbp.
- [L] 06/README.md:621: add a snippet for the pivot duplicate pair. List the duplicates with `duplicated(subset=..., keep=False)` and catch `ValueError: Index contains duplicate entries, cannot reshape`. Assignment Task 3.2 requires it.
- [L] 06/README.md:619: cut the 190-word narration to one sentence. At :336-338, replace the L04 recap with a direct definition of the index. At :480, move the Tidy Data citation to BONUS. Drop the :12 'Assignment 6' opening line.
- [L] 06/BONUS.md:16-25 and :687-739: delete the outline and the recap/Bottom Line sections. Change the :175-180 card to 'item: purpose'.
- [D] Move demo3:117-140 (set_index/reset_index) into Demo 2. After demo1:206, add row growth, then a caught `MergeError` from `validate='many_to_one'`, then an `indicator=True` re-merge (06-A). Add stated counts to Demo 2 ('survey_long has 12 rows', `.equals()` True) plus the intentional pivot ValueError (06-09). Add a Demo 3 concat of frames with differing columns under join outer vs inner.
- [D] Fix wrong claims. demo1:319-320: Monitor (3.1) is lowest. demo3:511-512: January is strongest. demo1:77: the merge is one-to-many (06-06). demo3:296-300: June is labelled 'actual' (06-07). Remove the DatetimeIndex partial-string slicing at demo3:131, :137, :176 (06-B). demo3:421-457: show the source_file column before keys=.
- [A] Put the exact header line in every README checkpoint: the lecture's own pattern at :315-317 keeps record_status and loses all of Task 1. Drop the final-cell asserts on unstated strings and the nonexistent 'dtype contract' (cell 3). Rewrite the README on 03's model.
- [A] Recast the soil/water/air specimens as clinical lab specimens, with baseline/follow-up SBP for Task 3. Regenerate fixture.json and _expected_rows.

## Lecture 07

The lecture is accurate and well-sized, but it opens with a story, has a misplaced comic, and ships a lie-factor figure that contradicts its own text. The demos practice little of blocks 1 and 3 and lean on BONUS-only seaborn. The assignment grader rejects the lecture's own vocabulary.

- [L] 07/README.md:22-26: open with the :28 contract definition and the four-statement list, and fold Nightingale into the image caption.
- [L] 07/README.md:61-63: write xkcd 1845's caption as '![xkcd 1845: State Word Map. If flexible method choices can produce any headline, the chart is not evidence.](...)', delete :63, and move the image before :117.
- [L] 07/media/tufte_lie_factor.png (07/scripts/generate_tufte_examples.py:129-140): regenerate it as the :578 hand-hygiene bar chart (95% baseline vs zero), with no emoji. The current image forces a line chart to zero, which contradicts :617.
- [L] 07/README.md:479: add `sns.heatmap(corr, annot=True, cmap='RdBu_r', center=0, vmin=-1, vmax=1)`. The :645 caption should say why the hatches are junk there, to contrast with :680 and Assignment Q2. Make the :626 and :655 captions state their point.
- [L] 07/README.md:119, :348, :456, :574: drop the allegory lead-ins. At :672-741, recast the 'prepared score' model examples on clinic SBP by visit (low priority).
- [D] demo3_pandas_altair.md: add a matplotlib first half that practices block 3: flawed clinic bar chart, then Tufte critique, then redesign (zero baseline, hatch, bar_label, outside legend), then a written text alternative and `json.dump`. End with `chart.save` on health rows (07-B, 07-03).
- [D] demo2_seaborn_statistical.md:144-245: replace violin/strip/reg/pair/jointplot (BONUS-only), suptitle, flatten and delaxes with healthexp scatter/line/box. Add corr, a grain lineplot and kdeplot `bw_adjust`, and add scipy to requirements (07-A, 07-18, 07-M2).
- [D] demo1_matplotlib_basics.md: rebuild with `fig, ax = plt.subplots()` (not pyplot style, :33-39) on clinic data. Drop polyfit, set_facecolor and colorbar, add units, and open with the contract/dtypes step (07-C, 07-19). Delete the stale tracked 07/demo/altair_*, scatter.* and interactive.html.
- [A] grading.py:16: accept 'ordinal' (the lecture's term, :50 and :739), which currently costs 38. Compare only the plotted columns (session_id costs 25). The README Q2 checkpoint must say the critique goes in output/visualization_evidence.json (37 pts). Drop the general-function, tie-rule and SHA-256 requirements (README:47, :57, :71).

## Lecture 08

The lecture is good apart from one misfiled intro and some caption issues. Demo 3 prints a hard-coded speedup and never practices the remote half of its block. Demos 1 and 2 use an employee table. The assignment grades column order.

- [L] 08/README.md:214-218: put the 'Missing Cells' paragraph under '## Totals and Absent Cells' as its intro and delete the ### heading. Drop `margins=True` from the first snippet (:196) so totals are taught once.
- [L] 08/README.md:89-104: before the snippet, add one sentence saying each named-aggregation keyword is an output column and its value is a (source column, function) pair.
- [L] 08/README.md:450, :498, :553: give xkcd 1319 a point sentence. Replace 2523 (Endangered, unrelated to SSH) with the unused media/xkcd_2533.png near :96-113. Remove or recaption punk.png.
- [L] 08/README.md:485: the '12.5 MB' figure needs pyarrow (53.5 without). At :521-531, add an `ssh-add` card row. Delete the :20 filler.
- [D] demo3_remote_performance.md:16, :278-318, :499-528: delete the unused Pool import, the hard-coded `sequential_time / 4` '4.0x' result, and the BONUS-only chunking (08-A).
- [D] demo3:320-387: replace the print-only Part 3 with terminal steps that have expected output: `ssh-keygen -t ed25519 -f ./demo_key -N ''` (never ~/.ssh), then tmux new/detach/`tmux ls`/attach. Rebuild Part 1 with `%timeit`, `agg('std')` vs a lambda, and category memory (08-06).
- [D] demo1/demo2:25-112: recast the salary table as synthetic clinic visits and re-derive every stated checkpoint. Replace the demo2:341-413 and :451-556 charts (axvline, yerr, transAxes, set_xticklabels) with printed checkpoints. The first filter at :426-432 must drop a group. Change demo1:145 `df.boxplot(by=)` to `sns.boxplot` (08-11).
- [A] grading.py:47, :84: compare column sets and reorder before checking values (a correct plain-string solution scores 76). Delete notebook cells 5 and 20, whose TODOs point to nonexistent values. Cut README:31-61 to one 'The data' paragraph. Make README and notebook numbering match.
- [A] Update or delete 08/POINTS.md, which is stale and links two BONUS anchors that do not exist.

## Lecture 09

All 21 snippet outputs are correct. Block 1 needs reorganizing, and two graded patterns need snippets. Demo 3 crashes on 'US/Eastern', and no demo practices the entity-aware material that most of the assignment grades.

- [L] 09/README.md:128-145: move the :145 `pd.date_range(...)` sentence above the alias card. Start a new `# Date Ranges, Frequencies, and Shifting` topic at :128, out of '# Date and Time Data Types' (demo placement is unchanged).
- [L] 09/README.md:892-897: define **chronological holdout** and add a 3-line snippet labelling the :844 `vitals` table at an 11:00 cutoff. It is graded in chronological_blocks.csv but taught only as a card bullet.
- [L] 09/README.md:357-391: add 'Reference Card: Bin Edges' (`closed='left', label='left'` for clock freqs; right/right for 'W'). Assignment 2.2 requires it.
- [L] 09/README.md:36-43: keep only the Regular/Irregular rows and move the rest to BONUS (971 lines, 3,363 words). Reduce :28 to one clause. Drop the :498 McKinney citation, and change the `*...*` at :49 and :130.
- [D] demo3_visualization_automation.md:286, :296, :306: change 'US/Eastern' to 'America/New_York' (crashes without tzdata-legacy). Localize in the recording zone and then convert to UTC, add the DST ambiguous/nonexistent case, and replace twinx, yerr, suptitle, violinplot, axvspan and rcParams.
- [D] Add patient_id to demo2:37-43 and a site grouping to Demo 3, and replace demo3 Part 6 (:317-383) with a grouped resample and asfreq grid, grouped shift/rolling `closed='left'`, a `resulted_at <= prediction_time` check, an `np.where` split, and the ungrouped-shift leak (09-A, 09-04).
- [D] demo1:295, :299: use the strict `<` form so the selection returns 72 rows, not 49 (09-16). Inject a weight episode so the demo1:357-402 checkpoint finds rows. In demo2, drop monitor hours and add a febrile episode, and use `.loc` at :249, :332, :365. Add the title/setup header and stated values to all three demos (09-B, 09-15).
- [A] README:102: write the Studio prediction times out in full (`2026-01-20 18:00:00+00:00`); the literal '18:00 UTC' scores 65. Cell 29 asserts `raw_readings` and `temporal_representation`, which are never defined, so drop them. Align the README and notebook numbering and cut the glossary preambles (README:31-42; cells 2, 11, 18).

## Lecture 10

The organization holds and all snippets verified. The prose around them has errors: one sentence runs backwards against its figure, four comic captions quote text the comics do not contain, and there are inline citations. The demos use untaught Altair and non-clinical data. The assignment deducts points for a refit that the lecture itself allows.

- [L] 10/README.md:33: the sentence is backwards. In the figure, moving right gains interpretability and loses predictive power (confirmed by viewing it). Rewrite it and move the :35 joke to after the card.
- [L] 10/README.md:20, :48, :672, :751: the captions quote dialogue the comics do not contain (confirmed for 2400, which is the vaccine/placebo 'Statistics tip'). Caption what each comic shows. Move 2169 near Leakage (:206-223), move or replace 2400, and give :202 xkcd 539 a point sentence.
- [L] 10/README.md:62, :76, :325: delete the '(McKinney Ch. 12.x)' parentheticals. At :648 and :656, drop the 'wins competitions'/'basically cheating' claims, which contradict :838.
- [L] 10/README.md:397-405: add a `pipeline.named_steps['model'].coef_` card row. Demo 2 uses it at :213-254 and :468.
- [D] demo1 Part 9: add `hour_sin`/`hour_cos` (or dayofweek, period 7) with a checkpoint showing hour 23 beside hour 0. No demo practices cyclic features, yet Assignment 11 grades them.
- [D] demo1:374-375: end the bins at `np.inf`; 308 rows currently drop silently (20332 vs 20640). At :365, AIC/BIC/adj R2 all favour Model 4. At :175-177, use association wording. Add expected values for Parts 2-8.
- [D] Redraw the residual plots in demo1 Part 4 and demo2 Part 8 with the lecture's ax.scatter/ax.axhline, and replace Altair layering, transform_regression, facet, resolve_scale, condition and domainMid (10-A). State the demo2:307 tie-break in Part 4. Drop the demo2:152-154 CV sentence and the demo3:591-599 emoji recap. Recast the housing/wine data (10-18).
- [A] grading.py:162-191: accept predictions from a refit on train plus validation, which README:570/:577 and both demos use (it currently costs 30), or state 'do not refit' in README 3.2. In `_table_values`, parse timestamps as UTC and compare keep/exclude case-insensitively and offsets/booleans numerically; parsed timestamps cost 65 and the lecture's 'Exclude (leakage)' costs 35.
- [A] The notebook cannot run to the end. Remove FIT_LEDGER, `_record_fit`, EXPECTED_PARTITION_IDS and the cell 39 snapshot asserts (cells 25, 29, 39), and change cell 9 to 'three CSVs'. Add a data section and a per-artifact acceptance note under each README checkpoint.

## Patterns across lectures

- Course-owned checks missing for 04 and 06-10 (verified: none of 04-10 has NN/assignment_checks/ and 0 of 7 workflows set CHECKS_REPO). One scripted change fixes all of them: create each NN/assignment_checks/ byte-identical to the vendored grading.py, check_assignment.py and .github/test/*, then port 03/assignment/.github/workflows/tests.yml with CHECKS_PATH 'NN/assignment_checks'. Every grading fix after that goes in both copies.
- _grader_selftest/ is tracked inside every handout 04-10, and scripts/publish_assignment.sh:64-66 rsyncs everything except .git and caches. 04/run.py:15-18 and 09/run.py:10-24 contain full reference answers. Move each set to NN/assignment_checks/_grader_selftest/ (03's layout) and add `--exclude '_grader_selftest/'` to publish_assignment.sh as a backstop.
- The graders are all-or-nothing and sensitive to formatting in 04 and 06-10 (and 05), against AGENTS.md:172. Scratch runs lost 24-65 points for a trailing space, header spacing, column order, 'ordinal' vs 'ordered', parsed timestamps, or capitalized labels. Use one shared approach: score per artifact (ideally per column or row), strip whitespace, compare parsed values (timestamps, numbers, booleans, casefolded labels), compare column sets rather than order, and report expected vs found.
- Notebook scaffolds gate students on things nobody told them. Version asserts appear in cell 1 of 05, 07, 08 and 09 (and in the 04 demos). Setup cells delete output/ in 07, 08, 09 and 10. Final readiness cells assert names or strings no instruction gives in 06, 07, 08, 09 and 10. Untaught hashlib, pathlib and ledger machinery appears throughout. The fix everywhere: print versions instead of asserting them, never unlink outputs, and delete the readiness/contract asserts or give every name and string in its TODO.
- The 04-10 assignment READMEs do not follow 03's task-first model. They lack fork/clone/git config setup, open with glossary preambles, use python -m venv as the main path, number tasks differently from the notebook, and give bare-filename checkpoints. Template them from 03/assignment/README.md, with the exact header line under each checkpoint.
- Non-health data: demos in 04 (2, 3), 05 (2, 3), 06, 07, 08 and 10 (housing/wine), and the assignments for 04 and 06-10. CLAUDE.md requires the demos to be recast. The 04-10 assignments are unpublished, so recasting their fixtures (with regenerated hashes and expected rows) is cheapest now.
- Demo conventions. No demo in 04-10 has an install cell, though the lectures link Colab: add a labelled `%pip install -q pandas==3.0.5` first cell per work/colab_standard.md. Delete the Learning Objectives/Key Takeaways lists in the 06, 07, 08 and 10 demos (10-22). Replace the legacy `np.random.seed` in the 06-10 demos (ALL-A) with `rng = np.random.default_rng(42)` and re-derive the stated numbers. Notebooks ship without outputs and most prose states no results, so add 'Expect:' lines after each checkpoint.
- Demos use untaught or BONUS-only tools: 04 (hashlib/urllib/importlib), 05 (concat/drop/heatmap before the break), 07 (violin/pair/jointplot, polyfit), 08 (psutil, time.time, yerr/transAxes), 09 (twinx, violin, rcParams), 10 (Altair layering and facets). Rule: each demo uses only the cards before its break; replace the tool or teach it in the lecture.
- scripts/lecture_lint.py misses an italic caption paragraph directly under an image (04/README.md:192, :518; 07/README.md:63). Add that rule.
- Topic intros open with stories or recaps against HANDOFF.md:69 (04:18, 05:20 and :577, 06:336-338, 07:22-26, 09:28), and inline McKinney/Wickham citations appear at 04:198, 06:480, 09:498 and 10:62/:76/:325. ASCII ' - ' dash stand-ins and `*...*` italics appear across the 04 and 06-10 READMEs, demos and BONUS files. Confirm the emdash agents own the dash sweep before editing.
- Spot-checks I ran, all confirmed: 10/README.md:33 contradicts its figure; the xkcd 2400 caption is wrong; the 06 BP-table promise vs the student-score snippets; `'ordered'` at 07/grading.py:16; the 08:214-218 misfiled heading; 'US/Eastern' in 09 demo3; no install cell in the 04 demos; test_assignment_grading.py:46-54 always runs the handout checker.

## Assignment 05 midterm checklist

- Create 05/assignment_checks/ and move grading.py, check_assignment.py and _grader_selftest/ into it. Fix run.py's paths and rerun it. scripts/grade_submissions.py:71-76 already prefers this path, and test_grade_submissions.py:309 still expects 85.
- From the handout, delete 05/assignment/.github/ (workflows/tests.yml and test/) and .badmath.toml ('Run `uvx badmath`...').
- assignment.ipynb: remove cell 16 'Supplied final verification' (audit-count, len==7 and assert_frame_equal asserts). Turn the cell 1 Python/numpy/pandas version asserts into printed versions. If the 570-byte SHA check stays, add .gitattributes with `data/people_raw.csv -text`.
- README :10, :160-168, :172, :186: remove every mention of the checker, GitHub Actions and 'including final newlines'. Word the points as '85 points graded from your committed files after the deadline, 15 by human review'.
- Check Your Work becomes a manual checklist giving each of the seven paths with its exact header and line count: raw_preview.txt 8, pipeline_summary.txt 5, numpy_age_summary.csv 6, pandas_selection.csv 4, issue_audit.csv 16, cleaned_people.csv 12, decision_log.csv 9.
- List all seven output paths in the file tree (:11) with the task that makes each. Print the exact 16-line issue_audit.csv in the README, because its labels currently live only in cell 16. Define source_sha256 as the sha256 in data/fixture.json. State `index=False` and needs_review True/False (an index costs 0/35). Make 'Foundation artifacts' (:37) a numbered task.
- Human review (:180): the three 5-point categories exist. Name the notebook cells each one reads and what earns full vs partial credit.
- Drop the required function names at :87-89, :122-123 and :129-131, or label them suggested; nothing grades them.
- Setup (:14-35): mirror 03 (fork, clone, git config, uv path, Windows note) and add a 'The data' section for data/people_raw.csv.
- In the course-side 05/assignment_checks/grading.py, stop chaining check_audit through check_foundations (grading.py:152-153; a missing newline costs 25). Score each foundation file separately, ignore trailing whitespace and the final newline, score cleaned_people.csv per record or column, ignore a leading index column, and report the first differing record_id and column in plain words.
- Update AGENTS.md:172 and HANDOFF.md:70 from 'shape checks only' to 'no automated checks in the handout; course-owned checks only'. scripts/test_assignment_grading.py:46-54 always runs NN/assignment/check_assignment.py and will fail once the handout checker is gone, so make it skip the handout checker for exams.
- Publish only on request: `scripts/publish_assignment.sh 05 UCSF-DataSci/ds217-26f-05`, after confirming that none of the removed files appear in the dry run.

## Decisions for the instructor

- Assignment 11: should the final follow 05's new rule (no automated testing in the handout)? AGENTS.md:172 and HANDOFF.md:70 currently say shape checks for both exams, and 11 still ships full checks.
- Assignment 05 publishes the exact contents of raw_preview.txt, pipeline_summary.txt, the numpy/pandas summaries and all 15 audit counts, so 25 of the 85 points can be copied. Is that intended for exam checkpoints?
- Recast the 04 and 06-10 assignment fixtures to health data now, while they are unpublished (fixture hashes and expected rows must be regenerated), or defer? Demo recasts are required by CLAUDE.md either way.
- Colab contract: add a `%pip install pandas==3.0.5` plus restart cell to every 04-10 demo, or relax the pandas==3.0.5 asserts and accept Colab's pandas 2.2 behaviour? 04/README.md:26 promises 'without installing anything'. Also decide whether 04 keeps Colab-only links (04-16).
- Assignment 10: should the grader accept both prediction sets (train-only and refit on train plus validation), or should README 3.2 forbid refitting?
- Assignment 08: should column order be graded at all? A correct plain-string solution scores 76.
- Scaffold engineering in 07, 09 and 10 (general functions for any input, SHA-256 read-back, tie rules, fit and prediction ledgers): delete it, or keep it as clearly optional?
- badmath: every .badmath.toml tells students to run `uvx badmath`, but the reviewer's uvx run and my `pip index versions badmath` both found no package (the sandbox network may be limited). Publish the package before handout, or remove the line.
- Lecture 08 Demo 3: is there a course server for a real ssh/scp/tunnel step, or should the demo stay local (ssh-keygen and tmux only)?
- Lecture 09 is 971 lines and 3,363 words. Should the Types of Time Series rows move to BONUS, or does the length stay?
- 07/POINTS.md and 08/POINTS.md are stale instructor cue sheets: update them or delete them? 06/README.md:12 is the only lecture opening with an 'Assignment N' line: remove it, or add the line to every lecture?
