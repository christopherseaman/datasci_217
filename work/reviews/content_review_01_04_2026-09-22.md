# Content review of demos and assignments 01-04 (2026-09-22)

Four reviewers each copied an assignment to scratch, stripped the solution, and completed it as a
student would - from the lecture and demo only, without reading the grader first - then ran
`check_assignment.py` on what they produced. The question was not whether the material runs; it was
whether it is worth a student's time and whether it assesses what the lecture taught.

**Every lecture came back WEAK.**

| Lecture | Verdict | High | Total |
| --- | --- | --- | --- |
| 01 | weak | 4 | 14 |
| 02 | weak | 5 | 14 |
| — | weak | 4 | 17 |
| 04 | weak | 4 | 12 |


## 01

**Verdict: weak**

### What the reviewer produced

Copied `01/assignment/` to `/home/christopher/projects/datasci_217/scratch/a01-content-review/work`, removed `__pycache__/`, `.github/`, `_grader_selftest/` (already student-state: `output/` held only `.gitkeep`, no `terminal-practice/`). Worked only from `01/assignment/README.md`, `01/README.md`, and `01/demo/DEMO_GUIDE.md`; did not open `grading.py`, `_assignment_checks.py`, or `check_assignment.py` until after producing every artifact.

Q1.1: pasted the ten given commands; `terminal-practice/source.txt` and `path-check.txt` created. Q1.2: `print("Python family:", python_family)` etc.; matched the shown output first try. Q2: wrote the loop from scratch modelled on demo 4.5; output matched the eight expected lines exactly first try. Q3.1: ran, fixed one error per run — IndentationError (line 5), NameError `participant_cout` (line 7), TypeError `participant_count_text + 1` (line 9) — three runs, clean on the fourth. Q3.2: `make_output.py` produced a correct 14-line `output/readiness.txt`. Q3.3: ran `capture_identity.py` with a `@ucsf.edu` address.

Grader result: **20/100.** `[PASS] terminal practice evidence`; `[FIX] committed readiness and identity artifacts: The identity hash does not match the course roster.` My report artifact was byte-identical to what the grader wants — substituting any hash from the shipped `ROSTER_HASHES` set gave `Score: 100/100. All checks passed.`

Places I had to guess or use something never shown: (1) `import`/`sys.`/`Path(`/`__file__` in the `readiness.py` supplied block — grep confirms 0 occurrences anywhere in `01/README.md` or `01/demo/`; (2) that "Count: 4" had to come from `len(measurements)`, since the nine-step list in 2.1 never mentions a count; (3) `input()`, used by `capture_identity.py`, also 0 occurrences in lecture or demo.

Then I ran three adversarial completions to test the grading contract. All three reached 100/100: hard-coding all three `readiness.py` values (which README:63 forbids); reconstructing `output/readiness.txt` and an identity hash purely by copy-paste out of the shipped `_assignment_checks.py`, writing no Python at all; and a bare directory containing only `terminal-practice/` + two forged files. Separately, running the README's literal `python3 make_output.py` under a non-3.13 `python3` (3.14.4 here) scored 20/100 with the message "output/readiness.txt must contain the documented 14-line readiness report" — no mention of the interpreter.

No repository file was modified; `git status --porcelain 01/` is empty.

### Findings

- **[high] too-easy** — `01/assignment/_assignment_checks.py:54`
    - The answer key ships inside every student fork. `_assignment_checks.py` is tracked (`git ls-files 01/assignment`) and must be, because `check_assignment.py` imports it. It contains `EXPECTED_READINESS` — the full 14-line report verbatim — at line 54, and all 40 `ROSTER_HASHES` at line 10. I built a directory containing only `check_assignment.py`, `grading.py`, `_assignment_checks.py`, an empty `terminal-practice/` with two `touch`ed files, and an `output/readiness.txt` regex-extracted from `EXPECTED_READINESS` plus the first roster hash. Result: `Score: 100/100. All checks passed.` Zero Python written, no loop, no debugging, no `make_output.py` run. Because the roster hashes are public in the student's own repo, the identity check also identifies nobody — any student passes with any classmate's hash, or with the first one in the set.
    - Fix: Move `EXPECTED_READINESS` and `ROSTER_HASHES` out of the student repo into the fetched test payload that CI already pulls, leaving `check_assignment.py` as a thin client that reports pass/fail without embedding the expected text. If local self-checking must work offline, ship a salted digest of the report (e.g. `sha256(report)`) rather than the report itself, and keep the roster list server-side only.
- **[high] instructions-disagree-with-grader** — `01/assignment/README.md:133`
    - The README tells students to run `python3 make_output.py` (README:133, and README:5 says "the commands below use `python3`"). `make_output.py` correctly uses `sys.executable`, so the report records whatever `python3` resolved to — and `_assignment_checks.py:54` hard-requires the literal line `Python family: 3.13`. On this machine `python3` is 3.14.4; running the README command verbatim produced `Python family: 3.14` and `Score: 20/100` with the message "output/readiness.txt must contain the documented 14-line readiness report." Nothing in that message mentions the interpreter. A week-1 student whose uv install didn't take effect, who is on macOS system Python 3.9, or on Ubuntu 24.04's 3.12, has three perfectly correct scripts, loses all 80 points, and is told their report is wrong. This is the single most common week-1 environment failure and the grader gives the least useful possible diagnostic for it.
    - Fix: Have `make_output.py` check `sys.version_info[:2] == (3, 13)` before running anything and exit with "This is Python {x.y}. Assignment 01 needs 3.13 — run `python3 --version`, then redo the uv install in Lecture 01." Add the same version hint to the `_assignment_checks.py:98` failure detail when line 1 of the report differs only in the version, and put an explicit `python3 --version` → expect `3.13.x` step in the assignment Setup section.
- **[high] cannot-complete** — `01/assignment/_assignment_checks.py:107`
    - The roster lookup and the report check are welded into one 80-point pass/fail (`check_output_artifact` asserts both; `POINTS = (20, 80)`). I produced a byte-perfect report and still scored 20/100 because my hash wasn't on the roster, and the output told me nothing about whether my Python was right. Any student added to the roster late, or whose roster address differs from the alias they type (the hash is over the punctuation-stripped local part, so `j.doe@ucsf.edu` and `jane.doe@ucsf.edu` are different people), gets 20/100 for correct work. The 3.3 checkpoint's own advice — "contact the course team" — concedes the student cannot resolve it. It is also the only part of the assignment a student cannot self-verify before pushing.
    - Fix: Split into three checks so the score is diagnostic: terminal practice (20), readiness report (60), identity (20). Then a roster mismatch costs 20, not 80, and the student can see their report passed. Better still, drop the roster set from grading entirely and use the hash only for instructor-side roster matching — identity is not a learning objective, and as shipped it is unenforceable anyway (see the answer-key finding).
- **[high] instructions-disagree-with-grader** — `01/assignment/README.md:63`
    - The README imposes source-code requirements the grader cannot and does not check, and then says so itself two pages later. README:63: "Use the supplied variable names, not repeated or hard-coded values." README:79: "use one direct `for` loop written as `for measurement in measurements:`". README:178: "The checker reads saved artifacts, not your source code or how you produced the results." I replaced all three `readiness.py` lines with literal strings — exactly what line 63 forbids — and scored 100/100. A student who reads carefully learns that the stated rules are optional; a student who reads carelessly may believe a working `while`-loop solution will be marked down when it won't.
    - Fix: Either drop the source-form mandates and rewrite them as guidance ("the loop form we want you to practise is…"), or state plainly under Check Your Work that these are practice instructions and grading is artifact-only. Do not present unenforced rules in the same voice as graded requirements.
- **[medium] too-easy** — `01/assignment/README.md:25`
    - The assignment exercises well under half of what the lecture teaches, and skips the shell half almost entirely. Never used: `cd`, relative vs absolute paths, `.`/`..`/`~`, `ls -la`, `cat`, `head`, `tail`, `echo`, `>`/`>>` redirection, writing and running a shell script with `bash` — that is the whole of lecture sections "Where You Are: Paths and the Working Directory", "Writing and Viewing Text Files", and "Create a Script by Pasting". On the Python side: no `while`, `range`, `enumerate`, `break`, `continue`, `type()`, string methods, booleans or `and`/`or`/`not`, no float or bool values, no `elif`, no `ValueError`. Question 1 is titled "Paths and readiness" and produces a file literally named `path-check.txt`, yet every one of its ten commands runs from the assignment root and the working directory never changes. Nothing checks a path.
    - Fix: Make Q1.1 actually about paths: have the student `cd terminal-practice`, create a file from inside it using `../` to reach back out, `pwd` at two depths, then build a small CSV with `echo`/`>>` and inspect it with `head -n 2` — all taught in the lecture and demoed in Demo 2. Add one `elif` branch or one `while`/`enumerate` use to Q2 so the control-structure block is exercised beyond a single `if`/`else`.
- **[medium] busywork** — `01/assignment/README.md:31`
    - Task 1.1 is 20 points — a fifth of the grade — for copy-pasting ten commands printed verbatim in the README, with no decision anywhere. The checker only asserts the two files are regular files and explicitly does not read their contents (`_assignment_checks.py:82-86`; Contract README:175 "contents are not checked"), so `mkdir terminal-practice && touch terminal-practice/{source,path-check}.txt` earns the same 20 points as doing the exercise. `cp`, `mv`, and `rm` leave no trace in the graded artifacts at all: the `cp`→`mv` chain exists only so `path-check.txt` appears, and `remove-me.txt` is created and deleted in the same breath with nothing to show for it.
    - Fix: Ask for something that requires a decision and leaves evidence. E.g. give a small messy folder in the repo and have the student reorganize it into `data/` and `results/`, then capture `ls -R` output into a committed text file — the artifact then reflects choices, and `cp`/`mv`/`rm` have a reason to exist.
- **[medium] busywork** — `01/demo/DEMO_GUIDE.md:436`
    - Demo 4.5 (`04e_measurement_workflow.py`) is the Question 2 answer with the nouns changed. Demo: `scores` / `passing_score` / `total` / `count` / `passing`, loop with `if score >= passing_score: status = "PASS" … else: status = "REVIEW"`, then prints total, count, average, passing. Assignment Q2: `measurements` / `review_threshold` / `total` / `len()` / `review_count`, `if measurement >= review_threshold: "review" … else: "within range"`, then prints Count, Total, Mean, Review count. The only substantive differences are dropping `enumerate` and using `len()` instead of a counter. Combined with the nine dictated sub-steps at README:77-85 — which specify the variable names, the loop header, the comparison direction, both labels, and the exact output — there is no judgement left. The student transcribes and renames.
    - Fix: Keep the demo as the worked example but make the assignment ask for a judgement the demo does not hand over: give the threshold as a clinical cut-off the student must pick a comparison direction for (is a low value or a high value the concern?), or give two thresholds and ask for three categories, so an `elif` and a decision are required. Cut the nine-step list down to the contract (inputs, required output lines) and let the student choose the construction.
- **[medium] demo-does-not-prepare** — `01/assignment/readiness.py:2`
    - The first thing the assignment asks a student to edit opens with six lines of machinery that appears nowhere in Lecture 01 or its demos. Grepping `^import |^from |sys\.|Path\(|__file__` across `01/README.md` and all of `01/demo/` returns zero hits. So `import sys`, `from pathlib import Path`, `sys.version_info.major`, `Path(__file__).name`, `str(...)` coercion, and the ALL-CAPS `PROJECT_LABEL` convention are all new to the student at the moment they open the file. "Do not edit this block" is not an explanation — a week-1 beginner reads it as "this part is magic", which is the opposite of what a first lecture should establish. Separately, `input()` (used by the supplied `capture_identity.py`, README:144) is also never taught: 0 hits in lecture or demo.
    - Fix: Replace the supplied block with values the lecture has actually taught — a plain `PROJECT_LABEL = "..."` string and, if the version really must be checked, have the student run `python3 --version` at the shell and type the observed value into a variable. That tests the same thing (did you install 3.13?) using only week-1 material, and removes the version-drift trap in the process. Alternatively add three sentences to the lecture's Python Basics section introducing `import` and one module, since later lectures need it anyway.
- **[medium] unrealistic-data** — `01/assignment/measurement_summary.py:1`
    - This is a health data science programme and neither the assignment nor the demo uses health data. The assignment's data is abstract to the point of meaninglessness: `measurements = [18, 21, 24, 19]` with `review_threshold_text = "20"` — 18 what? 20 of what? If a reader does take them for BMI (the one clinical number the lecture uses), flagging values at or above 20 as "review" is clinically backwards. `debug_report.py:9` computes `next_checkpoint = participant_count + 1`, i.e. adds one to a headcount to get a checkpoint number, which is not an operation on anything. The demo is worse: grepping `01/demo/DEMO_GUIDE.md` and `01/demo/*.py` gives 128 school-roster tokens (score 92, student 17, assignment 8, grade 7, exam 4) against 10 health tokens — 7 of which are a single BMI snippet and 3 are the word "measurement" in a filename. The lecture itself does better (systolic 118/142/131 at README:337, a clinic CSV at README:305), so the assignment and demo throw away framing the lecture had already established. This is the same defect already found in the Lecture 02 and 03 demos; Lecture 01's demo has it too and was not converted.
    - Fix: Recast Q2 as four systolic readings for one patient with a 140 mmHg cut-off — same loop, same `if`/`else`, same accumulator, and the threshold direction becomes obviously correct. Recast `debug_report.py` around an enrolment count and a next visit-window number, or anything where `+ 1` means something. Convert the Demo 4 scripts from `scores`/`students` to vitals or encounters, matching what was done for 02 and 03.
- **[medium] ambiguous** — `01/assignment/README.md:84`
    - Question 2's nine-step specification never tells the student to compute or print a count, but `Count: 4` is a required output line. Step 2 names only `total` and `review_count` as zero-initialised; step 8 says "calculate the mean using the actual list length"; step 9 says "print the summary labels shown below" — and the labels are not below, they are in the next subsection (2.2, README:92-99). A student following 2.1 top to bottom has no `count` variable and no list of labels in front of them. I had to scroll forward and infer that Count is `len(measurements)`. Worse, the demo they were just shown (4.2, 4.5) computes count with `count = count + 1` inside the loop, so the natural transfer conflicts with step 8's "actual list length".
    - Fix: Add an explicit step ("report how many measurements there were, using `len()`") and move the expected-output block from 2.2 up into 2.1 immediately after the step list, or change "shown below" to "shown in 2.2". Say which of the two counting approaches you want, or say that either is fine.
- **[medium] demo-does-not-prepare** — `01/demo/DEMO_GUIDE.md:404`
    - The debugging demo and the debugging assignment cover different error types. Demo 4.4 (`04d_debugging.py`) walks the student through NameError, TypeError, and ValueError. The assignment's `debug_report.py` contains IndentationError, NameError, and TypeError. So the student meets IndentationError cold in graded work, and the ValueError they practised is never assessed. IndentationError is the hardest of the four for a beginner precisely because, as the lecture explains at README:844, nothing runs at all and there is no `Traceback` header — it does not look like the other errors. The only mention of it in the demo is a warning that it is a *mistake* the student might make while uncommenting ("Deleting only the `#` leaves the line indented, and Python reports `IndentationError: unexpected indent` instead of the error you are looking for"), which frames it as noise to avoid rather than a diagnosis to practise.
    - Fix: Add a fourth commented case to `04d_debugging.py` that produces an IndentationError, or swap `debug_report.py`'s TypeError for a ValueError so demo and assignment cover the same three. Either way the graded errors should be a subset of the demonstrated ones.
- **[low] ambiguous** — `01/assignment/README.md:8`
    - The scaffold tree lists seven files; eleven tracked files plus a directory actually appear in the student's clone. Missing from the tree: `process_email.py`, `grading.py`, `_assignment_checks.py`, `test_assignment.py`, and `_grader_selftest/`. `process_email.py` is not optional — `capture_identity.py` imports it — yet the Contract at README:178 says "Extra files are ignored", which reads as permission to tidy up. A student who deletes the unexplained files breaks Task 3.3 with an ImportError they have no way to interpret in week 1.
    - Fix: List every file that ships, with a one-line annotation, or mark the grader internals with a leading underscore convention the README explains once ("files beginning with `_` are course machinery; leave them alone"). `process_email.py` should be renamed `_process_email.py` or folded into `capture_identity.py` so there is one supplied helper, not two.
- **[low] ambiguous** — `01/assignment/_grader_selftest/README.md:1`
    - `_grader_selftest/` ships to students and is purely instructor-facing: "This development QA smoke test copies the current assignment into ignored `scratch/`… graders use the public checker from a trusted copy." It references a `scratch/` directory the student does not have and describes a grading workflow that is none of their business. CLAUDE.md's assignment conventions say to avoid instructor-facing notes in assignments.
    - Fix: Move `_grader_selftest/` out of `01/assignment/` into `scripts/` alongside the other course tooling; nothing in it is for students.
- **[low] busywork** — `01/assignment/README.md:110`
    - Two sentences read as orphaned notes rather than instructions. README:110 — "Here, the list holds values for your loop. Lecture 02 covers lists, indexing, and slicing in more detail; Lecture 03 introduces NumPy arrays." — sits after the run-and-compare step with no connection to the task, and forward-references two lectures for material the student does not need. README:126 — "Test another participant count if helpful, then restore…" — invites an experiment without saying what to notice; changing `participant_count_text` to `"3"` makes the `if participant_count >= 4:` branch stop firing and the first output line vanish, which is the interesting thing and goes unmentioned.
    - Fix: Delete the forward reference at 110. At 126, name the observation: "Set it to 3 and rerun — the `Readiness: complete` line disappears because the `if` condition is now False. Set it back to 4 before generating the report."

### What works

The prose is clean and genuinely beginner-pitched — no filler, no instructor voice, the scaffold tree with per-file annotations is the right shape, and the Completion Contract is honest about exactly what it checks (I verified each clause against `_assignment_checks.py`; all four are accurate, including "Both artifacts must pass").

Task 3.1 is the one genuinely good exercise in the set. Three prepared errors, one per lecture error type, arranged so each is only reachable after the previous fix — I hit them in exactly the intended order and the 3.13 "Did you mean: 'participant_count'?" hint landed on the NameError. It teaches the read-fix-rerun loop the lecture's Debugging section describes, and the expected final output (README:120-124) lets a student confirm they got it.

Task 2's loop is the right difficulty for week 1: accumulate, branch, count, divide. My first attempt matched all eight lines, which means the specification is precise enough to hit without guessing at formatting.

Point totals are internally consistent: README's 20 + 80 matches `grading.py` `POINTS = (20, 80)` and the two checks in `PUBLIC_CHECKS`. The identity-hash design (hash locally, never commit the address) is a thoughtful privacy choice, and `make_output.py` correctly uses `sys.executable` rather than shelling out to whatever `python3` resolves to.

Checkpoints 1.1 and 3.2 are well-formed: both name the exact files and say what to look at ("check all 14 lines", assembled from the three per-task outputs).

## Lecture 02 (`git gud` with Version Control) — demo (`/home/christopher/projects/

**Verdict: weak**

### What the reviewer produced

I copied `02/assignment/` to `/home/christopher/projects/datasci_217/scratch/l02-student-run`, removed `__pycache__/`, `.pytest_cache/`, and `_grader_selftest/`, and completed it from `02/README.md` + `02/demo/` only, without reading `grading.py` or `_public_checks.py` first. Wrote `analysis_utils.mean`/`format_summary`, `main.py`, the two-line `.gitignore`, a 105-character README description, `python3 main.py` in a bash fence, and four `term; term` lines in `GIT_STATE_CHECK.md`. `python3 main.py` printed the four expected lines; `python3 -c "import main"` was silent. `python3 check_assignment.py` gave **100/100, all three checks PASS, on the first attempt** (Python 3.13.14 from `scratch/py313-demo-env`).

I then probed the grader in `scratch/l02-probe` (A-G). The decisive result: **Probe A scored 100/100 with every Python scaffold untouched** — 5 TODOs still in `main.py`, 7 in `analysis_utils.py`, 2 in `.gitignore` — and `report.txt` hand-typed by copying the block the assignment README prints verbatim at lines 105-109. No Python is written, read, imported, or executed by the grader at any point.

Points where I had to guess or the instructions ran out: (1) whether the git answers should be bare terms or sentences — bare terms is the only accepted form, and Probe C (grammatical sentences) and Probe B (one trailing period, `diff.`) both score 70/100; (2) which directory `report.txt` goes in — Part 3 step 3 (README:90) just says "Open `report.txt`", and Demo 3 models `output/vitals_report.txt`; Probe G (following the demo) scores 60/100; (3) that a blank line is required between `## Project description` and the text — Probe F scores 70/100 for a perfectly valid description, with a message about length and the word "measurement" that points at the wrong thing.

I also ran both demos end-to-end: `functions_demo.py` (with `120` typed) and `module_usage_demo.py` both match DEMO_GUIDE.md transcripts exactly, and `import module_usage_demo` is silent as documented.

### Findings

- **[high] busywork** — `02/assignment/README.md:127`
    - All 100 points are text artifacts. `analysis_utils.py` and `main.py` — Parts 2 and 3.1, the entire programming content of the assignment — are worth 0 and are never read, imported, or executed by the grader. Probe A: I scored 100/100 with all 14 TODO comments still in the scaffolds and `report.txt` created by copy-pasting the block the assignment prints at README:105-109. A student can finish this in three minutes without writing a line of Python, and the checker will tell them 'All checks passed.'
    - Fix: Make the artifact impossible to produce without running the code. Ship a small CSV of readings (like the demo's `clinic_vitals.csv`) that the student must read, and have `report.txt` contain per-record values derived from it — ideally with one unusable row so the `None` branch is earned rather than typed. Alternatively give each fork a different data file. Remove the verbatim expected-bytes block from the README, or keep it as a format example with different numbers.
- **[high] busywork** — `02/assignment/GIT_STATE_CHECK.md:5`
    - Every one of the four questions ends by naming its own answer: 'Use `working tree` and `diff`.' The grader (`_public_checks.py:53-62`) requires exactly those terms, in that order, split on semicolons. 30 of the 100 points are for retyping nine words that are printed on the same line as the question. This is the only assessment of the lecture's largest topic block.
    - Fix: Delete the 'Use `...`' clauses and grade against an accepted-synonym set, or replace the vocabulary quiz with a judgement: show a real `git status --short` / `git log --oneline` transcript and ask which state each named file is in and which single VS Code action moves it to the next one.
- **[high] instructions-disagree-with-grader** — `02/assignment/_public_checks.py:34`
    - The regex is `## Project description\n\n(.*?)` — a blank line after the heading is mandatory and is never mentioned anywhere in the instructions. Probe F: a 105-character description containing 'measurement', placed on the line directly under the heading, scores 0/30, and the message says 'Write a 30-300 character project description containing the word `measurement`' — which the student has already done. They will edit the one thing that is correct. Probe E is worse: a student who writes a real README the way the lecture's own Markdown snippet models it (02/README.md:690-701, `## Overview` / `## Run`) also scores 0/30.
    - Fix: Relax the regex to `## Project description\s*\n+` (same for `## Run`), and make the failure message report what it actually found: the captured length, whether the word was present, and whether the heading was located at all.
- **[high] instructions-disagree-with-grader** — `02/assignment/_public_checks.py:62`
    - The git answers are exact string equality after casefolding. Probe B: `1. working tree; diff.` — one trailing period — scores 0/30. Probe C: grammatically correct sentence answers ('The edit is in the working tree; the diff shows it line by line') score 0/30. All four lines share one message, 'Complete the four documented Git state answers.', which never says which line failed or why. Meanwhile README:134 describes the requirement as 'Four numbered, semicolon-separated answers ... using the terms for each snapshot' — 'using the terms' reads as permitting prose that contains them.
    - Fix: Match with `term in answer_part` after stripping punctuation, or state the exact required form in GIT_STATE_CHECK.md ('replace each TODO with the term alone, no other words and no punctuation') and report the first mismatching line number and its expected term in the detail message.
- **[high] unrealistic-data** — `02/assignment/main.py:8`
    - `{"label": "Morning", "values": [18, 21, 24]}` — the assignment never says what is measured or in what units. `grep -iE 'mmhg|patient|clinic|vital|systolic|blood|respirat|pulse|health|encounter'` across README.md, main.py, analysis_utils.py and GIT_STATE_CHECK.md returns zero hits. The student is then asked to write 'a 30-300 character description of what this measurement-summary project does' about a project with no subject. Nothing can be sanity-checked: 21.0 of what? The demo next door is now fully clinical (systolic mmHg, the 130 mmHg hypertension threshold, patient IDs) — the conversion stopped at the demo folder.
    - Fix: Recast the three records as one patient's systolic readings by time of day, with units in the output line (`Morning mean: 128.3 mmHg`) and the Overnight record empty because the cuff was not run overnight. That also gives the 'no measurements' branch a reason to exist and gives the student something real to write a description about.
- **[medium] demo-does-not-prepare** — `02/assignment/README.md:90`
    - Part 3 step 3 says only 'Open `report.txt` in text write mode' — no directory. Demo 3, the only model the student has, does the opposite: `Path("output")`, `mkdir(exist_ok=True)`, `output/vitals_report.txt`. Probe G: a student who follows the demo's pattern scores 60/100 with 'Commit report.txt as a regular file.' The disambiguating phrase ('at the assignment repository root') is 39 lines later in the Completion contract, and the failure message says nothing about location.
    - Fix: Say 'in this assignment directory, beside main.py' in step 3, and make the check report the paths it searched when `report.txt` is missing but `output/report.txt` exists.
- **[medium] demo-does-not-prepare** — `02/demo/DEMO_GUIDE.md:183`
    - Demo 3's whole subject — reading a CSV, `try`/`except ValueError`, blank-row and extra-field guards, `assert` checkpoints, `pathlib.Path` — is exercised by nothing in the assignment. In the other direction, the assignment's single most fragile requirement (a Markdown file with exact headings and exact blank-line placement, 30 points) is rehearsed in no demo at all. The lecture also spends 02/README.md:180-197 on resolving merge conflicts and the Submit section (README:139) tells students to 'Resolve any unexpected conflict', but Demo 1 merges cleanly and never produces one, so a student who hits a conflict at submission time has read about it and never done it.
    - Fix: Either pull the demo's skills into the assignment (give it a CSV with a bad row) or add a short fourth demo step that deliberately creates and resolves a conflict in `notes.md`, plus one that writes and previews a README.
- **[medium] too-easy** — `02/assignment/README.md:50`
    - Nothing in the assignment asks for a decision. Part 1.3 prints the exact two lines of `.gitignore` to type; Part 3 prints the exact stdout and the exact bytes of `report.txt`; `main.py`'s TODO comments name the variable (`report_file`), the mode (`"w"`), and the method call (`report_file.write(report_text)`); the records are supplied and marked do-not-edit. The one real idea — `0.0` is a measurement and `None` is not (README:76-77) — is handed over as a required example rather than discovered, and is worth 0 points. This is transcription, not practice.
    - Fix: Remove the exact-output block or change its numbers, strip the per-line TODOs down to a docstring contract, and add one step that requires a choice with consequences — e.g. a record whose values include a `0` and a record that is empty, with the student deciding how each should read in the report and defending it in the README description.
- **[medium] busywork** — `02/assignment/README.md:139`
    - The Setup and Submit sections direct the student through create-branch, stage, commit with specific messages, publish, switch to main, merge, resolve, sync — the lecture's largest block and Demo 1's entire content. None of it is checked by anything. A student who never creates `feature/measurement-summary`, never branches, and commits once directly on `main` scores an identical 100/100. The instructions ask for real work and then signal, by scoring it zero, that it does not matter.
    - Fix: Either check it (the grader runs in the student's repo — `git log --oneline` can confirm a merge commit or the two required commit subjects exist) and give it points, or stop presenting unassessed steps as requirements and label them clearly as practice.
- **[low] ambiguous** — `02/assignment/README.md:65`
    - Part 2.1 says 'accumulate the values with a `for` loop and return the total divided by `len(values)`', which forbids `sum()` without saying why. The lecture's own canonical function (02/README.md:449-453) and the demo's `mean_reading` both use `sum(readings) / len(readings)`, and the lecture's reference card lists `sum(grades)` as the way to total a collection. A student who follows the lecture is told by the assignment that they did it wrong, and nothing enforces either way.
    - Fix: Drop the constraint and let them use `sum()`, or say what the loop is for ('write the accumulation by hand once so you can see what `sum()` does') — and note that it is a practice instruction, not a graded requirement.
- **[low] ambiguous** — `02/assignment/README.md:1`
    - The deliverable `README.md` is the assignment instruction sheet. The student adds a project description and a Run command to a document that also contains Parts 1-3, a Completion contract, and Submit instructions, and then commits it as the repository front page. The lecture (02/README.md:674) teaches that README.md is 'a note saying what it is and how to run it, shown below the file list on the front page' — the artifact the student ships directly contradicts the thing they were just taught a README is.
    - Fix: Put the instructions in `ASSIGNMENT.md` and ship a short `README.md` skeleton with just the two headings, so the graded artifact is actually the kind of document the lecture described.
- **[low] too-easy** — `02/assignment/_public_checks.py:11`
    - `_public_checks.py` ships in the student repo and is not listed in the README's Files tree. It contains `EXPECTED_REPORT` verbatim and the exact four-tuple of git terms. Strictly nothing in it is secret — both are already printed in README:105-109 and in the GIT_STATE_CHECK questions — but a student who opens it sees, in twenty lines, that the assignment is three strings and no code.
    - Fix: Low priority on its own; it resolves itself once the graded artifact actually depends on running the code. If kept visible, list it in the Files tree so it is not a surprise.
- **[low] ambiguous** — `02/demo/DEMO_GUIDE.md:22`
    - Demo 1 still has the student type `Experiment: compare two grade summaries.` — leftover roster framing in a demo whose other two thirds are now clinical. It also does not match the lecture's conflict illustration, which shows `compare median grades` against `compare three grade summaries` (02/README.md:187-189), so a student comparing the two wonders whether they are meant to be the same scenario.
    - Fix: Use the same line in both, in clinical terms: e.g. `Experiment: flag systolic at 120 instead of 130.` in the demo, and make the lecture's conflict block the current/incoming pair of that same line.
- **[low] ambiguous** — `02/assignment/README.md:84`
    - Part 3 contains a single subsection numbered '3.1 Complete the driver' with no 3.2. Cosmetic, but a beginner scanning for the rest of Part 3 loses a moment looking for it.
    - Fix: Drop the number, or fold the report checkpoint into a 3.2.

### What works

The demos are genuinely good and they are not the problem. `functions_demo.py` walks one morning's clinic log and hits every item in the lecture's second block in order — tuple unpacking for the fixed `(clinic, date)` record, a list that grows when a walk-in arrives, dict `.items()`/`.get()`, `input()` with a defaulted empty answer, and a set intersection — with the "copy this loop into every script" version shown immediately before the imported helper, which is the clearest possible motivation for a function. The `mean_reading([0, 0]) -> 0.0` beat next to `mean_reading([]) -> None` earns its place: a pain score of 0 is a real answer, and that is exactly why the test has to be `is None`. `module_usage_demo.py` gives a CSV with `not recorded` in a numeric column and handles three distinct malformed-row shapes, each explained before the code. Both scripts execute and match their guide transcripts byte for byte, `assert` is introduced as a visible checkpoint and then deliberately made to fire ("Watch the checkpoint fire"), and the "If the script cannot find the data" section pre-empts the single most common beginner failure. The demo guide is also independently completable: clone instructions, a raw-download fallback, and per-step expected output. The three demo breaks map cleanly onto the three lecture blocks. The assignment's own grader behaves sanely where it was designed to: the run-command regex accepts `python`, `python3.13 ./main.py`, `py -3.13`, fenced or inline, wrapped Markdown paragraphs, and CRLF `report.txt` (Probe D, 100/100).

## 03 — NumPy Arrays & Virtual Environments (demo + assignment)

**Verdict: weak**

### What the reviewer produced

Copied 03/assignment/ to scratch/l03-review/student/, removed __pycache__ and .github, and completed it from 03/README.md, 03/demo/DEMO_GUIDE.md and the 01-02 lectures only (grader read afterwards).

Task 1 and Task 2 were pure transcription — the README dictates the exact file contents and the exact four pipeline commands, so there was nothing to decide. Task 3.1-3.7 were also transcription: the README gives the literal return dict for all seven functions, including `array = np.array(values, dtype=np.float64)`.

Task 3.8 (the driver) is where real work started, and it is under-specified. The README never states the arguments to pass: I had to reverse-engineer threshold=30 from the print labels, offset=5 by subtracting the "Adjusted values" line from the data, rows=3/columns=4 from the shape of the printed "Grid", that `values`/`baseline` are `measurements[:, 1]` and `measurements[:, 0]`, and that "that copy's first middle value" means `source[1, 0]`. I also had to pick the transcript's print order over the numbered order in 3.8, because the two contradict each other.

Result: `python check_assignment.py` gave **60/100** (env 20/20, pipeline 40/40, analysis 0/40) for a fully correct implementation. Diffing against the grader's constant showed exactly 3 wrong lines out of 30:
  expected 'Second column: [20. 30. 20. 20. 30. 40.]'  / produced '[20. 30. 40. 20. 30. 40.]'
  expected 'Mask at or above 30: [False  True False False  True  True]' / produced '[False  True  True False  True  True]'
  expected 'Selected values: [30. 30. 40.]' / produced '[30. 40. 30. 40.]'

Those three lines cannot be produced by any correct implementation. The row means force each row of the 6x2 to sum to 30/50/70/30/50/70 and the column means force col1 to sum to 180, but the demanded second column sums to 160. Independently of the CSV, the vector_operations block contradicts itself: "Adjusted values: [25. 35. 45. 25. 35. 45.]" forces offset=5 and values=[20,30,40,20,30,40], which cannot give mask [F,T,F,F,T,T].

I then confirmed the converse: I deleted `array_analysis.py` and `analysis.py` entirely, wrote `output/analysis.txt` by hand from the README's printed transcript, and `check_assignment.py` returned **100/100, "All checks passed."** with no NumPy written at all.

### Findings

- **[high] cannot-complete** — `03/assignment/_public_checks.py:24`
    - The graded transcript is arithmetically impossible. A correct implementation of the seven documented functions run on the supplied observations.csv scores 60/100. Three of the thirty required lines are wrong: 'Second column: [20. 30. 20. 20. 30. 40.]' (line 24), 'Mask at or above 30: [False  True False False  True  True]' (line 35) and 'Selected values: [30. 30. 40.]' (line 36). The fixture's follow_up column is [20,30,40,20,30,40]. The expected block is also self-contradictory independent of the data: the demanded second column sums to 160 while the demanded 'Column means: [20. 30.]' requires 180, and 'Adjusted values: [25. 35. 45. 25. 35. 45.]' forces offset=5 with values=[20,30,40,20,30,40], which cannot produce mask [F,T,F,F,T,T]. The identical wrong lines appear in README.md:234, 245 and 246, so a student has no correct source to work from.
    - Fix: Write a reference solution, run it against observations.csv, and regenerate both EXPECTED_ANALYSIS and the README transcript from its real stdout. Never hand-author the expected block again.
- **[high] busywork** — `03/assignment/grading.py:10`
    - Nothing the student writes is graded. I deleted array_analysis.py and analysis.py, hand-typed output/analysis.txt from the README, and scored 100/100. All 40 analysis points hang on byte-equality with a constant that the README prints in full — and since that constant is unreachable by correct code, typing it is not merely the easy path, it is the only path to full marks. The entire NumPy content of the lecture, which is the lecture's subject, is worth zero.
    - Fix: Fix the transcript first, then make the graded artifact something a student cannot transcribe: have analysis.py write a small derived result (a count, a mean, a list of flagged patient IDs) from a fixture the student is not shown the answers to, or supply two fixtures and grade output from the one that is not worked through in the README.
- **[high] ambiguous** — `03/assignment/README.md:213`
    - Task 3.8 never states the arguments for the helpers, yet the artifact is graded on exact output. I had to infer threshold=30 from a print label, offset=5 by subtracting the 'Adjusted values' line from the data, rows=3 and columns=4 from the shape of the printed 'Grid', and that `values` and `baseline` for vector_operations are columns 1 and 0. A beginner who cannot do that reverse-engineering cannot start, and the checker's one-line failure message gives no clue which guess was wrong.
    - Fix: State the calls explicitly, e.g. 'call vector_operations(measurements[:, 1], measurements[:, 0], threshold=30, offset=5)' and 'call reshape_and_transpose(measurements, 3, 4)'.
- **[high] instructions-disagree-with-grader** — `03/assignment/README.md:216`
    - The numbered order in 3.8 contradicts the graded transcript. The list says (4) 'print the four selections, the vector-operation results, and the reshape/transposed arrays' then (5) 'print the view and copy before and after'. The transcript, which the grader enforces line by line, puts the view/copy block before the vector-operation results. A student who follows the numbered instructions produces the right values in the wrong order and scores 0 on that 40-point check.
    - Fix: Renumber step 4/5 to match the transcript: selections, then view/copy, then vector operations, then reshape/transpose.
- **[medium] instructions-disagree-with-grader** — `03/assignment/README.md:280`
    - The Completion Contract says analysis.txt needs 'every transcript line shown in Task 3, in that order' — a containment test. The grader does `_lines(...) == EXPECTED_ANALYSIS`, exact tuple equality. I added the two-line banner that all four demo scripts model ('NumPy Analysis' / '==============') and dropped from 100 to 60. Every demo teaches the banner style, then the assignment silently forbids it.
    - Fix: Say 'exactly these 30 lines and nothing else — no banner or blank lines', and note that the demos' banners are not wanted here.
- **[medium] instructions-disagree-with-grader** — `03/assignment/README.md:30`
    - requirements.txt already ships complete. Git HEAD content is 'numpy==2.3.3\n', but 1.1 says 'Replace the single TODO line in requirements.txt' and the file tree at line 7 labels it an 'environment record to complete'. A student opens it, finds no TODO, and has to decide whether they are looking at the wrong file or have already been given the answer. (.python-version does correctly contain TODO.)
    - Fix: Ship requirements.txt containing TODO like .python-version, or drop 1.1's requirements step and the tree annotation.
- **[medium] ambiguous** — `03/assignment/README.md:223`
    - 'Start with the existing six summary lines, then print these labeled learning results' refers to lines that do not exist. analysis.py's main() is `pass`; there is no existing print anywhere in the scaffold. A student reads this as 'the scaffold already prints something' and hunts for code that isn't there. It looks like leftover wording from an earlier version where main() was partly written.
    - Fix: Delete 'existing' — 'Print exactly the following 30 lines, in this order.'
- **[medium] unrealistic-data** — `03/assignment/observations.csv:1`
    - The assignment data is a toy fixture, not health data: columns site/baseline/follow_up, six rows of 10/20, 20/30, 30/40 repeated twice, sites north/south/west. No units, no clinical quantity, no plausible range. Every row's follow-up is exactly baseline+10, which is why 'Difference from baseline' prints [10. 10. 10. 10. 10. 10.] — a degenerate dataset in which no analytic question can be asked. The demo is fully clinical (diastolic mmHg, stage thresholds, clinic case mix); the assignment regresses to a fixture chosen for tidy arithmetic.
    - Fix: Recast the fixture as the demo already is: clinic, systolic_mmhg at visit 1 and visit 2, ~12 rows with realistic spread, and let the thresholds in the task be the clinical ones (140 for systolic stage 2) rather than 30.
- **[medium] too-easy** — `03/assignment/README.md:91`
    - There is no decision anywhere in Tasks 1-3.7. The README gives the exact contents of .python-version and requirements.txt, all eight environment commands, all four pipeline commands verbatim, and the literal return dict for every one of the seven functions including the expression for each value. I produced 27 of the 30 graded transcript lines on the first attempt without thinking about NumPy at all. At week 3 a student should still be choosing a mask, an axis, or a threshold.
    - Fix: Give the clinical question and let them choose the operation: 'report how many readings are at or above the stage-2 systolic threshold' rather than 'return np.sum(flattened >= threshold)'. Keep the dict keys as the contract; drop the bodies.
- **[medium] demo-does-not-prepare** — `03/assignment/README.md:126`
    - The assignment ignores roughly half the lecture, and specifically the half the demo spends most of its time on. Exercised: venvs/requirements, the tail|cut|sort|uniq pipeline, array properties, 2D indexing, views/copies, one mask, broadcasting, mean by axis, reshape/transpose. Never touched: list comprehensions, zip/isinstance/dir (the whole 'Python Tools for Collections' topic and demo 2.1), np.where (demo 3.2 and 3.4), argsort/argmax ranking (demo 3.3), default_rng/seed, ufuncs, astype on numeric text, combining masks with & and |, and sum/std/min/max. Conversely a student who did only the demo has never written a function returning a dict, never run `python script.py > file.txt`, and never used `.python-version`/`uv python pin` — the demo's recreation check copies requirements.txt only.
    - Fix: Add one task that makes a np.where label column and one that ranks with argsort, both of which the demo already models; and have the demo's recreation step copy .python-version too.
- **[medium] unrealistic-data** — `03/demo/demo3_bp_analysis.py:19`
    - The docstring says 'Create realistic diastolic blood pressure data' but the generator is `rng.integers(70, 101, size=(100, 5))` — uniform. The run therefore reports 85 of 100 patients in stage 1 hypertension and only 6 normal, which is not a population any clinic sees. DEMO_GUIDE.md:363 then narrates pure noise as findings: 'the five patients a clinic would follow up first' and 'Patients whose reading rose: 49 / Average rise: 9.8 mmHg' are artifacts of independent uniform draws with no patient effect and no visit effect. Health data science students are exactly the audience that will read a signal into this.
    - Fix: Draw a per-patient mean (e.g. standard_normal around 78 with sd 8) plus per-visit noise so the distribution is plausible and the ranking is stable, or keep the uniform draw and say in the guide that the ordering and the rise are noise by construction.
- **[low] ambiguous** — `03/assignment/_public_checks.py:86`
    - The only feedback on a 40-point check is 'analysis.txt must contain the documented NumPy learning transcript.' With 3 wrong lines out of 30 — which is what a correct student gets today — a beginner has nothing to act on and no way to find which line differs without reading the grader source.
    - Fix: Report the first differing line number with both the expected and the found text.
- **[low] ambiguous** — `03/assignment/README.md:266`
    - 'Check your work' says to run `python analysis.py` then `python check_assignment.py`, without the redirect. The checkpoint at line 259 is the only place that says `python analysis.py > output/analysis.txt`. A student who edits analysis.py and follows the Check-your-work block sees the new output on screen but grades the stale file, or no file at all on a first run.
    - Fix: Use `python analysis.py > output/analysis.txt` in the Check-your-work block too.
- **[low] instructions-disagree-with-grader** — `03/assignment/README.md:91`
    - PIPELINE.md is required in Task 2 ('Replace the four TODO lines') and in Submit ('stage the environment records, PIPELINE.md, and all six output text files'), but nothing reads it. A student can leave all four TODOs in place, generate the four output files by any means, and score 100. The Completion Contract is honest about this — it does not list PIPELINE.md — which makes the two sections disagree.
    - Fix: Either check PIPELINE.md contains the four commands, or stop asking for it and let the output files be the deliverable.
- **[low] cannot-complete** — `03/assignment/_grader_selftest/run.py:32`
    - The self-test validates the grader against its own constant: it does `write(output / "analysis.txt", EXPECTED_ANALYSIS)` and asserts 100. It never runs a reference solution, so the impossible transcript passes every check and has survived repeated review. This is the mechanism behind the top finding, not an incidental detail.
    - Fix: Keep a reference array_analysis.py/analysis.py in _grader_selftest/, run it in the self-test, and assert its stdout equals EXPECTED_ANALYSIS.
- **[low] unrealistic-data** — `03/README.md:317`
    - Residual school-roster framing in the lecture: names = ['Alice','Bob','Charlie'] with grades = [85, 92, 78] (lines 317-323), 'a grades table gives a per-student average and a per-assignment average' (652), 'a flat run of 12 grades becomes 3 students by 4 assignments' (687), and `scores = rng.integers(70, 101, ...)` (801). The corresponding demo, demo2_python_collections.py, is already clinical (patient IDs and bpm), so the lecture snippet is now less clinical than the demo it introduces.
    - Fix: Swap for patient IDs with a clinical measure, matching the demo the snippet leads into: 12 readings becoming 3 patients by 4 visits.
- **[low] ambiguous** — `03/README.md:217`
    - Task 2 requires `tail -n 2 observations.csv` (last two lines), but the lecture's pipeline reference card only defines `tail -n +2 FILE` as 'skip the first line'. Plain `tail -n N` is never taught, and demo1_cli_pipeline.sh only uses `tail -n +2`. A student who reads the card and sees `tail -n 2` in the assignment will reasonably read it as the header-skipping form.
    - Fix: Add `tail -n 5 FILE`: Print the last five lines' to the Pipeline Building Blocks card next to `head -n 5`.

### What works

The demo is genuinely good and is the strongest piece here. All four demo scripts run clean on Python 3.13 / NumPy 2.3.3 and every output block in DEMO_GUIDE.md matches the real run byte for byte — I diffed all four. The framing is properly clinical: diastolic mmHg with correct stage thresholds (below 80 normal, 80-89 stage 1, 90+ stage 2), systolic bands in demo3_csv_summary using the different systolic cutoffs and saying so, mmHg-to-kPa conversion, a cuff calibration offset, heart rates in bpm, fever at 100.4 F, mg-to-g dose conversion. The 1,500-row encounters.csv has a deliberately constructed case mix (Nephrology 140.3 mmHg vs Obstetrics 115.3) and the guide explains why. Demo 3.4 closes the loop by running the same count through the shell pipeline and through the array and showing the two agree — that is a real teaching move, not decoration.

The lecture itself is well built: incremental ordering, an analogy per concept (supply cabinet for venvs, pill organizer for ndarray), visuals before code, and reference cards that cover what the demos use. The views/copies and boolean-mask topics in particular lead with the failure mode.

Point totals are internally consistent: the README contract table is 20/40/40 and grading.py's POINTS is (20, 40, 40). `uv python pin 3.13` really does write "3.13\n", so the lecture's claim and the grader's exact-match check agree — verified with uv 0.12.1.

## 04

**Verdict: weak**

### What the reviewer produced

Copied `04/assignment/` to `/home/christopher/projects/datasci_217/scratch/a04-student-review/`, removed `_grader_selftest/`, `.github/`, and `__pycache__/`, and completed `assignment.ipynb` using only `04/README.md`, `04/demo/`, and lectures 01-03 — without opening `grading.py` or `check_assignment.py` first. Task 1: moved the producer cell above the dependent cell and wrote the state explanation. Task 2: built the Series/DataFrame, named the index, took the bracket/`.loc`/`.iloc` selections. Task 3: read the CSV, built the mask, `.loc` + `.copy()`, derived `line_total`, sorted, wrote with `index=False`, read back.

Ran it top-to-bottom in a fresh interpreter (`scratch/py313-demo-env`, Python 3.13.14, pandas 3.0.5, NumPy 2.3.3) with `scratch/a04-student-review/run_all.py`, which simulates restart-and-run-all. Clean run, then `python check_assignment.py` → **100/100 on the first attempt, no guessing, no grader consulted**. Zero points where I had to invent anything: every statement I wrote was either printed verbatim in `assignment/README.md` or lifted from `demo2_pandas_basics.md` / `demo3_data_io.md` with the labels renamed.

That flawless first pass is the problem, not the reassurance. Three counterfactuals, all run:
- **No notebook at all.** In `scratch/a04-exp-byhand/` I typed the two CSVs by hand from the README's own text (the Completion Contract prints `site-102,15,23` / `site-103,10,17`; §3.2 prints the nine purchase IDs; `data/purchases.csv` supplies the rest) with no `assignment.ipynb`, no pandas, no Python. Checker: **100/100, All checks passed.**
- **Wrong mask.** In `scratch/a04-exp-mask/` I changed `>= 2` to `> 2`. The notebook's own "Final fresh-run verification" cell printed **"Assignment 04 fresh-run verification passed"**; the checker then said **40/100**.
- **Untouched starter.** 0/100 with clear messages, and the scaffold correctly raises `NameError: name 'base_rate' is not defined` — Task 1's intended first experience works.

### Findings

- **[high] busywork** — `04/assignment/README.md:89`
    - The README prints the answer. Of the ~15 statements a student writes, 9 appear verbatim in the instructions — `baseline_series = measurement_table["baseline_c"]` (:78), `baseline_table = measurement_table[["baseline_c"]]` (:79), the entire `label_block = measurement_table.loc["site-102":"site-103", [...]]` block (:89-92), `.iloc[1:3, 0:2]` (:95), `purchases = pd.read_csv(DATA_PATH)` (:109), `quantity_at_least_two = purchases["quantity"] >= 2` (:115), the four source column names (:121), `by=["line_total","purchase_id"]` and `ascending=[False,True]` (:135-136). The remaining ~6 are demo 2 and demo 3 with labels renamed. §3.2 then prints the nine output IDs (:141) and the Completion Contract prints the two labeled_block rows (:166). Verified: in scratch/a04-exp-byhand/ I typed both CSVs by hand from the README, with no notebook, no pandas and no Python in the directory, and scored 100/100 'All checks passed.' The student makes no decision anywhere in this assignment — not a threshold, not a column, not a sort key, not an output path. A good week-4 task still asks for a judgement.
    - Fix: Stop publishing the answers and ask for one decision. Keep the artifact schema in the contract but delete the nine IDs at :141 and the exact row values at :166. Then give the student a question instead of a formula: 'Which encounters were billed for more than one unit, and which five cost the most? Choose a sort that gives the same five in the same order on every run and say in Markdown which column you used as the tie-breaker and why.' Grade the artifact as now; the answer is no longer in the handout.
- **[high] unrealistic-data** — `04/assignment/data/purchases.csv:2`
    - The assignment is an office-supply order. USB Cable, Mouse Pad, Desk Lamp, Keyboard, Webcam Cover, Cable Tie, Screen Cloth (`_grader_selftest/alternate_fixture` is the same: Marker Set, Pencil, Tape Roll, Label Pack). Counted: `assignment/README.md` has 0 health-domain terms to 22 retail terms; `assignment.ipynb` 0 to 25; the lecture it assesses has 113 to 5. Task 2 is worse than neutral — README:73 names the columns `baseline_c` and `follow_up_c` and :66 names the Series `reading_c`, and the values are 12/18, 15/23, 10/17 and 12.5-15.5. If `_c` is Celsius those are physiologically impossible body temperatures (fatal hypothermia is below 28 °C), in a lecture that correctly uses 36.8 and 38.1 five sections earlier. The demos share it: demo 2 has literally zero health context (its four 'health' keyword hits are the word 'Colab' matching 'lab '), and demo 3's main dataset is Anscombe's quartet. This is the same defect just flagged in the Lecture 02 demos, sitting in Lecture 04's assignment.
    - Fix: Recast purchases.csv as a dispensing or charge log — `encounter_id, medication, doses_dispensed, unit_cost_usd` → `total_cost_usd` — keeping 12 rows, nine at quantity >= 2 and the same tie structure so the grader needs only new expected values. For Task 2, make `baseline_c`/`follow_up_c` a real paired measurement with real units: baseline and follow-up systolic mmHg (148→132, 126→118, ...) or HbA1c %, indexed `P001`-`P004`.
- **[high] instructions-disagree-with-grader** — `04/assignment/assignment.ipynb:249`
    - The notebook's own 'Final fresh-run verification' cell gives a false green. Its asserts are self-referential: `assert len(round_trip) == int(quantity_at_least_two.sum())` compares the file against the student's own mask, and `expected_order` at :255 re-sorts `round_trip` and compares it to itself. Verified in scratch/a04-exp-mask/: a student who writes `> 2` instead of `>= 2` sees the cell print 'Assignment 04 fresh-run verification passed', then the checker reports 40/100. The cell the README describes as giving 'immediate feedback' tells them they are right when they are wrong on the higher-value task. Separately, `reading_by_site`'s index and name are specified in a full bullet list at README:64-67 and checked only by `assert isinstance(..., pd.Series)` at :235 — get the labels or the name wrong and both the notebook and the grader say 100.
    - Fix: Replace the self-referential asserts with literal expectations the student can be wrong about: `assert len(round_trip) == 9`, `assert round_trip['purchase_id'].tolist() == [...]` is off the table if the IDs leave the README, so use `assert round_trip['line_total'].is_monotonic_decreasing` plus `assert round_trip['quantity'].min() >= 2 and purchases['quantity'].eq(1).sum() + len(round_trip) == len(purchases)`. Add `assert reading_by_site.name == 'reading_c'` and `assert list(reading_by_site.index) == [...]` if that Series is going to stay.
- **[high] too-easy** — `04/assignment/README.md:112`
    - The assignment assesses about half the lecture, and skips the half that matters most for health data. Exercised: Series/DataFrame construction, bracket selection, `.loc`/`.iloc`, one Boolean mask, one derived column, `sort_values`, `to_csv`/`read_csv`. Ignored entirely: `na_values` and missing markers (lecture :526-578, a full demo-3 section), `isna().sum()`, `value_counts()`, `duplicated()`, `nunique()` (the whole 'First-look inspection' reference card at :610-642 plus demo 3 §'Take a first look'), `df.info()`, `df.describe()`, combining masks with `&`/`|` (lecture :400, demo 2's last two sections), `df.loc[mask, col] = value` flag columns (lecture :454, demo 3), magic commands, and `display()` vs `print()`. The instruction at :112 is 'Inspect its shape, columns, dtypes, and first rows' — and `purchases.csv` is 12 rows with no blanks, no missing markers, no duplicates and no categorical column, so there is nothing for inspection to find. Task 2's `measurement_values` even contains a duplicated row (`[15,23]` at site-102 and site-104) that is never mentioned.
    - Fix: Ship the fixture with real defects: one `?` in the numeric column, one blank, one row entered twice, and a clinic/site column. Then require the three answers as part of the artifact — a second small CSV or added columns — so the student must read with `na_values=["?"]`, report `isna().sum()` per column and `duplicated().sum()`, and decide whether the repeat row belongs in the selection. That is the week-4 skill this lecture actually teaches.
- **[medium] instructions-disagree-with-grader** — `04/assignment/README.md:31`
    - Task 1 is worth zero points and the README never says so. It is presented as one of three numbered tasks with two subtasks, including the only writing in the assignment. `grading.py` scores exactly two artifacts (40+60), and `check_assignment.py`'s docstring says 'student notebook code is never executed or inspected'. Verified: scratch/a04-exp-byhand scored 100/100 with no `assignment.ipynb` in the directory at all. So a student can skip the cell reorder and the state explanation entirely and lose nothing, and a student who does them gets no feedback and no credit. The only part of this assignment that asks for a judgement is the only part that is unscored — exactly backwards.
    - Fix: Either make it count or say it does not. Preferred: have Task 1.2 write its explanation to a committed text artifact (`output/state_notes.md`) and score presence plus a couple of required terms, moving to 20/40/40; or add one line under Task 1 — 'This task is not scored; it is the habit that keeps Tasks 2 and 3 reproducible.'
- **[medium] too-hard** — `04/assignment/assignment.ipynb:18`
    - The first cell a week-4 beginner opens is the hardest code in the assignment and is never explained. Fifty lines using `hashlib.sha256`, `json`, `Path.resolve()`, a hand-rolled upward directory search with a `while True` and a nested `for`, a `set(manifest) == required_manifest_fields` comparison, and seven asserts. Lectures 01-03 teach `assert` (02/README.md:613) and `Path` (02/README.md:545) but never `hashlib`, `json`, `.resolve()`, or recursive path search; the Lecture 04 README mentions none of them. Demo 3 at least labels its equivalent 'the supplied bootstrap' and says what it does in prose before the cell; the assignment README describes cell 1 nowhere — its file tree at :5-12 does not mention it. A beginner who hits `Could not find data/purchases.csv and data/fixture.json from this launch directory` has 50 lines of unfamiliar code to interpret.
    - Fix: Put demo 3's one-paragraph framing in front of the cell: name it setup, say it locates the input, verifies its checksum, creates `output/`, and defines `DATA_PATH`, `LABELED_OUTPUT_PATH`, `SELECTED_OUTPUT_PATH`, and say the student does not need to modify or read it. Add one recovery line to the README: if the assert fires, run `%pwd` (Lecture 04) and open the notebook from the assignment folder.
- **[medium] ambiguous** — `04/assignment/README.md:127`
    - `line_total = quantity * unit_price` is inside a ```python fence but is not runnable Python — `quantity` and `unit_price` are bare names that do not exist. Every other python fence in this README is copy-pasteable and correct (:38, :78-79, :89-92, :109, :115, :135-136), so a beginner has been trained by the preceding page to paste this one, and gets `NameError: name 'quantity' is not defined`. The instruction also never says what to name the assigned column or that it must be assigned back onto the copied frame.
    - Fix: Either make it real — `selected_purchases["line_total"] = selected_purchases["quantity"] * selected_purchases["unit_price"]` — or move it to a ```text fence and write it as the formula it is: `line_total = quantity x unit_price`. Given the busywork finding, the better fix is a text fence stating the rule and letting the student write the pandas.
- **[medium] busywork** — `04/assignment/README.md:64`
    - Three of Task 2's required objects are dead ends. `reading_by_site` is specified over four lines (index labels and name) and is then used only in one `print(... .index, .dtype, .name)`; it is never selected from, never saved, and never graded. `baseline_series` and `baseline_table` are given verbatim as code and checked only by `isinstance`. So 40 points ride on two rows of one CSV, while three of the five constructed objects exist to be typed. The data is also incoherent inside the one cell: the Series index is `north/south/east/west`, the DataFrame index holds `site-101`-`site-104`, and that index is named `record_id` — three different naming schemes for what the prose calls sites.
    - Fix: Cut `reading_by_site` or give it work: make it the per-site mean the student computes from the table, or drop it and let Task 2 be one coherent table. Make the Series/DataFrame return-type contrast pay off by requiring both in the artifact — e.g. write `baseline_table` to CSV and have the checker reject the Series-shaped file — so the double-bracket distinction has a visible consequence. Use one label scheme: `P001`-`P004` under index name `patient_id`.
- **[medium] ambiguous** — `04/assignment/README.md:99`
    - Neither Checkpoint tells the student what they should see. Both restate the task ('Save the two selected rows with the named record_id index and both measurement columns'; 'Save the nine selected purchases ... with index=False') rather than showing the expected file or printed output, so the checkpoint cannot confirm anything on its own — the student has to scroll to the Completion Contract at :166 for the actual values. The demos do this right (demo 3 gives 'no gaps, four datasets of eleven rows, no repeated records' inline). The project convention is explicit: 'Give students concrete expected results so they can tell what changed and whether it worked.'
    - Fix: Show the first two lines of each expected file under its checkpoint, or — better, given the answers should not be published — show the shape and header only: 'After this cell: `labeled_block.csv` has 3 lines, first line `record_id,baseline_c,follow_up_c`' and 'round_trip.shape is (9, 5) and round_trip.columns lists five names ending in line_total'.
- **[low] instructions-disagree-with-grader** — `04/assignment/README.md:173`
    - The Submit section contradicts the lecture it assesses. Lecture 04 §'Before You Commit a Notebook' (:178-184) gives a three-step ritual — Clear All Outputs, check for sensitive data, save — and the demos state that canonical notebooks are committed with outputs cleared and execution counts removed. The assignment's Submit paragraph asks only to 'inspect the notebook and CSV diffs and confirm they contain only synthetic course data' and says nothing about clearing outputs, so students commit a notebook full of stored output in the same week they are taught not to.
    - Fix: Add the lecture's step 1 and 3 to Submit: 'Clear All Outputs, save, then check the diff.' One sentence, and it makes the lecture's privacy rule a habit rather than a slide.
- **[low] cannot-complete** — `04/assignment/README.md:20`
    - Setup says 'install the two deliberate notebook dependencies' and `requirements.txt` holds only numpy and pandas — no `ipykernel`. `04/demo/requirements.txt` by contrast pins jupyterlab and jupytext. Selecting a fresh `.venv` as a VS Code notebook kernel needs ipykernel in that environment; VS Code prompts to install it, so the student recovers, but the instructions as written do not produce a runnable notebook and the phrase 'notebook dependencies' points at numpy and pandas, which are not notebook dependencies. Not verified in VS Code from this environment — flagged from the requirements asymmetry.
    - Fix: Add `ipykernel` to `04/assignment/requirements.txt`, or add one line to Setup: 'VS Code will offer to install ipykernel the first time you select this environment as the kernel — accept it.'
- **[low] busywork** — `04/assignment/_grader_selftest/README.md:1`
    - `_grader_selftest/` is tracked in git (7 files, including `autograder.py`, `grader_core.py`, `protected_files.json`, and an `alternate_fixture/`) but is absent from the README's annotated file tree at :5-12, which lists only assignment.ipynb, the environment records, data/, the checker, and output/. Its README is instructor-facing prose shipped to students — 'Instructors run a trusted copy of that checker against submissions' — which the course conventions rule out ('Avoid preambles, forbidden-code lists, and instructor-facing notes'). A student browsing their repo finds an undocumented folder holding a second fixture and something called an autograder.
    - Fix: Keep it out of the distributed assignment, or list it in the file tree with a one-line student-facing gloss ('course QA for the checker; ignore'). I did not verify how the standalone student repository is generated, so if that process already excludes it this is moot.

### What works

Not padding — these are real:

- **`04/README.md` (the lecture) is the strongest piece and is fully clinical.** 113 health-domain terms vs. 5 non-health. Patients P001-P003 with age/temp_c/smoker, fever masks at 38.0 °C, Celsius-to-Fahrenheit derivation, a tie broken on systolic 142. Visuals precede code throughout (the ASCII Series-inside-DataFrame diagram at :211, the mask diagram at :388, the derive-then-sort diagram at :430). Outputs are correct for pandas 3.0.5 specifically (`dtype='str'`, not `object`), and the Copy-on-Write `ChainedAssignmentError` note at :466 is right for pandas 3.
- **Demo 1 teaches kernel state by making the student break it.** §"Repair the hidden dependency" walks them through producing a stale `36`, then `24`, then a `NameError` from a fresh kernel, by hand. That is the correct way to teach this and it is rare.
- **Demo 3's determinism section is a genuine demonstration, not an assertion.** It sorts the same seven rows from two different starting orders and shows `x` alone yields `I, II, III` one way and `II, III, I` the other, then shows the second key collapsing both to one order. A student can see why the tie-breaker exists.
- **Checker messages are specific and actionable**, and name the task: "Missing output/labeled_block.csv; complete Task 2 and create the committed artifact."
- **The stated point split is honest where it applies.** README's 40/60 table matches `grading.py: POINTS = (40, 60)`; "Column and row order matter; numeric values are compared with tolerance" matches the grader's exact `fieldnames` comparison and `isclose(rel_tol=1e-9)`.
