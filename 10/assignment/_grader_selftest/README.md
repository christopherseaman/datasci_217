# Assignment 10 instructor self-test

This directory is instructor-only and must never enter a learner submission.

`run.py` materializes a correct notebook only in disposable directories, executes it through the real Jupyter entry point, checks exact artifacts and live behavior, exercises public alternates and adversarial package boundaries, and validates official Classroom50 result behavior.

The release-required `constraints.txt` is intentionally absent. Until the course-wide exact transitive lock and immutable central-container digest are supplied, `autograder.py` returns a nonzero infrastructure failure. The candidate harness uses only the exact direct candidate environment and makes no release claim.
