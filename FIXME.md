# Lecture 02 follow-up

- [ ] Align Assignment 02 run instructions with `python3 main.py`; accept equivalent valid Python commands in the README artifact check instead of deducting 30 points for `python3`. Update the assignment repository when publishing the fix.
- [ ] Restore the two missing demo breaks in `02/README.md`, placing every demo after its prerequisites and keeping the final demo last.
- [ ] Fix the Git demo: make and commit a change on `experiment`, switch to `main`, then merge. The current sequence merges the branch into itself.
- [ ] Replace the demo helper's list comprehension with a taught loop; remove or introduce the unexplained `raise` statement.
- [ ] Correct “branches come later” to match branching within Lecture 02.
- [ ] Explain newly introduced string methods rather than presenting them as review; describe `None` as a value, not a type.
- [ ] Simplify the functions/module demos so report generation and validation do not obscure the reuse lesson.
